apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: celeste-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: celeste-service
            port:
              number: 8080
